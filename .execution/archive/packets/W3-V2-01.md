# Packet State

- packet: `W3-V2-01`
- title: `Final Steps Before E2E and Experimental Preview`
- status: `approved`
- assigned agent: `codex-WORKER-v2-final-steps`
- branch: `feat/rewrite-ontology`
- worktree/container: `canonical repo workspace` / `local`
- depends on: `W3-RUNTIME-01`, `W3-TEAM-01`, `W3-STARTUP-01`
- unblocks: `local binary smoke test`, `persona E2E run`, `experimental preview go/no-go`
- last transition: `2026-04-07`
- next Codex action: `commit the docs bundle and dispatch W3-E2E-01`
- latest review: `2026-04-07-W3-V2-01-codex-recheck`

## Notes

- this is the release-prep and operator checklist packet, not a product rewrite
- it should turn the current approved/queued state into one concrete sequence:
  - rebuild/install the current binary
  - run local smoke tests on that binary
  - run the persona E2E packet
  - state the remaining preview caveats explicitly
- if `W3-RUNTIME-01` is still unresolved when this packet starts, the output must
  make that caveat the first gating item instead of hand-waving it away
- `W3-RUNTIME-01` is now approved and committed in `45529f8`, so this packet's
  review should verify that the audit/checklist no longer treats runtime as
  pending
- detached Codex review `2026-04-07-W3-V2-01-codex` found one blocker only:
  the packet-owned audit/task docs still describe `W3-RUNTIME-01` as pending
  even though runtime is already approved and committed in `45529f8`
- detached Codex re-review `2026-04-07-W3-V2-01-codex-recheck` approved the
  packet:
  - no blocking findings remain
  - final-steps docs now start from `W3-RUNTIME-01` already approved and
    committed in `45529f8`
  - rebuild/install guidance, BP-09 operator/developer split, and preview
    caveats all re-verified cleanly
