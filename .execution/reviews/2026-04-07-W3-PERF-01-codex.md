# W3-PERF-01 Codex Review

## verdict

- `needs_codex`
- No packet-owned code blocker surfaced in the scoped diff. The approval blocker is evidence: the representative real-dataset RSS result is only recorded as a heartbeat claim in `.execution/agents/codex-WORKER-codex-ingest-rss-budget.md:9-16`, not as a durable, reviewable validation artifact.

## scope of review

- reviewer session: `codex-REVIEWER-codex-ingest-rss-budget`
- reviewed execution/control-plane inputs: `docs/execution/00-global-rules.md`, `docs/execution/01-dispatch-protocol.md`, `docs/execution/05-live-control-plane.md`, `docs/execution/tasks/W3-PERF-01-codex-ingest-rss-budget.md`, `.execution/program.md`, `.execution/blueprints.md`, `.execution/packets/W3-PERF-01.md`, `.execution/packets/W3-E2E-01.md`, `.execution/agents/codex-WORKER-codex-ingest-rss-budget.md`, `docs/execution/audits/2026-04-07-W3-E2E-01-persona-local-postgres.md`, `docs/solutions/2026-04-07-codex-ingest-timeouts-pin-bundles.md`, `docs/solutions/2026-04-08-rss-shutdown-poisons-local-sqlite-store.md`
- reviewed packet-owned BP/code/tests: `docs/blueprint/BP-02-data-flow.md`, `docs/blueprint/BP-07-process-lifecycle.md`, `src/adapters/codex.ts`, `src/pipeline/ingest.ts`, `src/pipeline/loop.ts`, `src/commands/benchmark.ts`, `test/codex-reference-adapter.test.ts`, `test/pipeline-spec-gap-closure.test.ts`
- fresh validation: `bun test test/codex-reference-adapter.test.ts test/pipeline-spec-gap-closure.test.ts` -> `7 pass`, `0 fail`

## blocking findings

1. Representative validation evidence is not strong enough for the packet claim.
   - The packet requires representative packet-local proof that Codex ingest no longer trips the `256 MB` hard limit and a clear statement on whether that is enough to rerun installed-binary E2E.
   - The only real-dataset evidence in the control plane is the heartbeat summary at `.execution/agents/codex-WORKER-codex-ingest-rss-budget.md:9-16` (`ingestOne() + temp SQLite store`, `163 refs loaded`, `peak RSS 186 MB`).
   - The committed tests only verify fixture-scale adapter parity, Codex one-ref batch cadence, and generic RSS enforcement; they do not capture the real `~/.codex` run, the measurement method, or the absence of the hard-limit log.
   - Until that evidence is written down durably enough to review, `W3-PERF-01` should not move to `approved`.

## BP Acceptance Matrix verification

- `Pipeline checks still enforce the BP-02 RSS warning/hard-limit behavior` -> verified in `src/pipeline/loop.ts:30-31` and `src/pipeline/loop.ts:342-365`; rechecked by `test/pipeline-spec-gap-closure.test.ts:177-242`.
- `Codex ingest work is batched or streamed so peak RSS stays within the budget on representative validation input` -> code path verified in `src/pipeline/ingest.ts:43-48`, `src/pipeline/ingest.ts:124-140`, `src/pipeline/ingest.ts:203-255`, `src/adapters/codex.ts:119-177`, and `src/adapters/codex.ts:844-1052`; fixture-scale coverage reran in `test/codex-reference-adapter.test.ts:20-148` and `test/pipeline-spec-gap-closure.test.ts:109-145`; representative-input proof remains insufficient for approval.
- `The fix does not widen adapter or sink contracts` -> verified by scope. No edits under `src/contracts/**` or `src/sinks/**`; the packet-owned diff is limited to `src/adapters/codex.ts`, `src/pipeline/ingest.ts`, and `test/pipeline-spec-gap-closure.test.ts`.
- `Runtime lifecycle semantics remain the same apart from avoiding kill-switch shutdown on normal ingest` -> verified. `src/pipeline/loop.ts:208-365` is unchanged, so the bounded-shutdown path stays BP-07-aligned; the diff only reduces Codex retention so normal ingest should avoid that path.

## V1 comparison

- Fixture-scale parity appears preserved. `test/codex-reference-adapter.test.ts:20-148` still proves stable ref IDs, message/tool IDs, compaction splitting, and spawned linkage.
- The only intended behavior change in the packet-owned diff is memory profile: lighter `findChanged()` indexing, streamed `loadConversation()` parsing, single-conversation Codex batch cadence, and a clearable timeout helper.
- Real-dataset parity is not independently documented beyond the heartbeat note, so this comparison is not yet fully evidenced on the representative dataset.

## aligned

- `src/pipeline/loop.ts` keeps the BP-02 warning at `200 MB` and hard limit at `256 MB`; no limit tuning or guard removal occurred.
- `src/pipeline/ingest.ts` narrows Codex batches to `1` and forces reclamation between Codex refs, which tightens rather than weakens the BP-02 batch boundary.
- `src/pipeline/ingest.ts:203-223` replaces the uncancelled `Promise.race()` timeout with a clearable timer, which addresses the documented timeout-retention issue without changing adapter or sink contracts.
- `src/adapters/codex.ts` moves discovery toward lightweight indexing and one-file load caching, with streamed full parsing in `scanJsonlFile()` and no cross-packet contract edits.

## drift

- Process drift: the worker handoff does not include a durable completion artifact for the representative `~/.codex` validation; the only packet-local memory result is still embedded in the agent heartbeat.
- Implementation specialization: Codex now hard-overrides the configured ingest batch size to `1` in `src/pipeline/ingest.ts:47-48`. That is tighter than the general BP-02 default of `20`, not looser, so I do not treat it as a blocker.

## unowned spread

- No packet-owned code spread found.
- The worktree contains unrelated in-flight edits in non-owned files (`src/commands/ingest.ts`, `src/commands/start.ts`, `src/db/store.ts`, docs), but this packet's scoped diff does not depend on them.

## progress

- Fresh focused validation passed: `bun test test/codex-reference-adapter.test.ts test/pipeline-spec-gap-closure.test.ts` -> `7 pass`, `0 fail`.
- The code path looks review-ready once the representative-memory proof is recorded durably enough to audit.
- `W3-PERF-01` should stay `review_ready` / `needs_codex`, not `approved`, until that proof is attached.

## Codex decisions needed

- Require the worker to add a durable representative-validation artifact: exact command or harness, dataset scope, peak RSS measurement method, loaded-ref count, and confirmation that the `256 MB` hard-limit log no longer appears.
- After that artifact exists, rerun review; I expect the packet can move to `approved` if the documented run matches the heartbeat claim and no new scope drift appears.
