# W3-PERF-02 Codex Review

## verdict

- `approved`
- no blocking findings remain in the current packet-owned diff

## scope of review

- Read the required execution docs, live control plane, packet state, worker
  heartbeat, packet audit, and packet-owned BP/code/tests.
- Re-checked:
  - `src/adapters/codex.ts`
  - `src/commands/watch.ts`
  - `src/db/bundle.ts`
  - `test/runtime-store-cutover.test.ts`
  - `test/db-store-spine.test.ts`
  - `docs/execution/audits/2026-04-08-W3-PERF-02-full-runtime-rss-shutdown-flush.md`
- Ran:
  - `bun test test/runtime-store-cutover.test.ts test/db-store-spine.test.ts`

## blocking findings

- none

## BP Acceptance Matrix verification

- `Runtime still enforces BP-02 warning/hard-limit semantics`
  - verified; `src/commands/watch.ts` now only specializes `pushBatchSize: 2`
    and no longer forwards runtime RSS env overrides into `runPipeline(...)`
- `Real workload no longer trips the hard limit during ingest-adapter / shutdown-flush on the installed/local runtime path`
  - verified by the refreshed audit and packet-owned runtime evidence
- `The fix does not widen frozen adapter or sink contracts`
  - verified; diff stays inside packet-owned runtime/adapter/store files and
    the remaining Railway error is still treated as out of scope
- `Single-owner daemon/service lifecycle semantics remain intact`
  - verified; no lifecycle ownership drift found

## V1 comparison

- parity kept on startup/watch ownership and sink routing
- intended behavioral delta remains limited to:
  - streamed Codex discovery
  - smaller runtime push batches
  - lower-allocation bundle hashing

## aligned

- the prior approval blocker is resolved:
  - `src/commands/watch.ts` no longer exposes
    `JIN_RSS_WARNING_MB` / `JIN_RSS_HARD_LIMIT_MB`
- the audit now matches the actual code shape and keeps the BP-02 guard frozen
- focused tests pass locally

## drift

- none blocking

## unowned spread

- none observed

## progress

- the packet now cleanly closes the real-runtime RSS evidence gap
- the remaining Postgres push failure is correctly separated into a sink-side
  follow-up rather than hidden inside this packet

## Codex decisions needed

- move `W3-PERF-02` to `approved`
- keep the Railway/Postgres push failure in the separate sink lane
