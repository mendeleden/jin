# W3-PERF-03 Codex Review

## verdict

- `approved`
- no blocking findings remain in the current packet-owned diff

## scope of review

- Read the required execution docs, live control plane, packet state, worker
  heartbeat, packet audit, packet-owned blueprints, and packet-owned code/tests.
- Re-checked:
  - `src/commands/benchmark.ts`
  - `test/perf-harness/README.md`
  - `test/perf-harness/run-v2-benchmark.sh`
  - `test/perf-harness/benchmark-v2.test.ts`
  - `test/self-observation.test.ts`
  - `docs/execution/audits/2026-04-08-W3-PERF-03-repeatable-v2-performance-harness.md`
- Ran:
  - `bun test test/perf-harness/benchmark-v2.test.ts test/self-observation.test.ts`

## blocking findings

- none

## BP Acceptance Matrix verification

- `The harness measures the real v2 discover/load/write/push path`
  - verified; the command surface is the v2 phase ladder, not legacy
    `sessions()` / `messages()`
- `Phase-level RSS attribution is explicit and repeatable`
  - verified; requested adapters now fail loudly when unavailable and
    `highWaterMarkBytes` / `summary.peakRssBytes` are normalized before
    persistence
- `The lane does not widen frozen adapter/store/sink contracts`
  - verified; diff stays inside packet-owned harness files
- `Future release candidates have a durable command/runbook to execute this harness`
  - verified by the runbook, wrapper, tests, and refreshed packet audit

## V1 comparison

- intentional BP-backed change:
  - removed the legacy benchmark surface
  - replaced it with phase-isolated v2 pipeline execution

## aligned

- the prior approval blockers are resolved:
  - requested adapters no longer disappear silently
  - peak RSS artifacts are normalized before publication
- focused tests pass locally
- the packet-local audit and README now match the stricter artifact contract

## drift

- `report.summary.verdict` remains an execution-success verdict rather than a
  release-budget verdict; this is informational and stays outside the packet
  because BP-10 defines approval semantics on top of the harness

## unowned spread

- none observed

## progress

- the harness is now durable enough to serve as the repeatable pre-release v2
  measurement surface this packet set out to build

## Codex decisions needed

- move `W3-PERF-03` to `approved`
- keep release-budget policy layered on top of this harness rather than folded
  into this packet
