# W3-SCALE-01 Codex Review

## verdict

- `approved`
- Codex can move `W3-SCALE-01` from `review_ready` to `approved`.

## scope of review

- Read the required execution guidance, packet, live control-plane files, packet-local audit, and the review surface named for this lane.
- Reviewed:
  - `docs/blueprint/BP-03-conversation-model.md`
  - `docs/blueprint/BP-04-adapter-contract.md`
  - `scripts/perf-datasets/generate.ts`
  - `scripts/perf-datasets/validate.ts`
  - `scripts/perf-datasets/clean.ts`
  - `test/perf-datasets/README.md`
  - `test/perf-datasets/scale-datasets.test.ts`
- Confirmed the packet was `review_ready` in `.execution/packets/W3-SCALE-01.md:3-13` and the worker reported generation plus validation completion in `.execution/agents/codex-WORKER-scale-datasets.md:16-18`.
- Ran `bun test test/perf-datasets/scale-datasets.test.ts` and observed `3 pass`, `0 fail`.

## blocking findings

- None. I found no blocker in the allowed review surface.

## BP Acceptance Matrix verification

- `Generated datasets preserve ontology-relevant structure like compacted chains and spawned children` -> verified. `BP-03` requires manifests to preserve `trace_id`, `parent_id`, and `relationship` semantics for compacted and spawned conversations (`docs/blueprint/BP-03-conversation-model.md:30-32`, `docs/blueprint/BP-03-conversation-model.md:76-120`, `docs/blueprint/BP-03-conversation-model.md:168-214`). The packet README states each manifest records per-adapter `expectedRefs` plus `traces` with trace, parent, and relationship metadata derived from adapter `findChanged()` / `loadConversation()` output (`test/perf-datasets/README.md:64-80`). Focused coverage asserts every scenario/tier retains compacted and spawned relationships and that a saved manifest round-trips through parser-backed validation (`test/perf-datasets/scale-datasets.test.ts:42-82`).
- `Dataset generation is deterministic from committed seeds` -> verified. `BP-04` requires lightweight discover/load refs and deterministic ref derivation instead of cached full bundles (`docs/blueprint/BP-04-adapter-contract.md:103-166`). The generator CLI is parameterized only by committed scenario and scale inputs (`scripts/perf-datasets/generate.ts:19-39`, `scripts/perf-datasets/generate.ts:42-120`), and the focused test asserts repeated `codex-heavy` `10x` generation produces identical manifests across two independent output roots (`test/perf-datasets/scale-datasets.test.ts:24-40`). The reviewed packet surface contains generator/test sources, not checked-in generated dataset blobs.
- `Scale tiers are consumable by future perf harnesses without live private data` -> verified. The packet contract requires `1x`, `10x`, and `100x` on-demand datasets and local commands instead of live tool directories (`docs/execution/tasks/W3-SCALE-01-deterministic-scale-datasets.md:7-11`, `docs/execution/tasks/W3-SCALE-01-deterministic-scale-datasets.md:60-75`, `docs/execution/tasks/W3-SCALE-01-deterministic-scale-datasets.md:92-98`). The README documents all three tiers plus one-command generate/validate/clean flows (`test/perf-datasets/README.md:19-62`), while `generate.ts`, `validate.ts`, and `clean.ts` expose the corresponding CLI entrypoints (`scripts/perf-datasets/generate.ts:19-39`, `scripts/perf-datasets/validate.ts:12-43`, `scripts/perf-datasets/clean.ts:18-31`).
- `The packet does not widen runtime or adapter contracts` -> verified. The packet explicitly forbids `src/**`, blueprint edits, and adapter/runtime contract changes (`docs/execution/tasks/W3-SCALE-01-deterministic-scale-datasets.md:41-58`, `docs/execution/tasks/W3-SCALE-01-deterministic-scale-datasets.md:76-81`). `git status --short --untracked-files=all -- scripts/perf-datasets test/perf-datasets docs/execution/audits/2026-04-08-W3-SCALE-01-deterministic-scale-datasets.md .execution/agents/codex-WORKER-scale-datasets.md .execution/blueprints.md .execution/reviews` showed only the packet-local script/test/audit files; no `src/**` or adapter/runtime contract files were introduced by this lane.

## V1 comparison

- `no prior v1 surface`
- This matches the worker audit and the packet’s own deliverable shape: the lane adds a new deterministic dataset toolchain rather than rewriting an existing v1 dataset surface.

## aligned

- The review surface matches the packet goal of replacing live private directories with deterministic, on-demand rich-adapter fixtures at `1x`, `10x`, and `100x` (`docs/execution/tasks/W3-SCALE-01-deterministic-scale-datasets.md:7-11`, `docs/execution/tasks/W3-SCALE-01-deterministic-scale-datasets.md:60-75`).
- The manifest contract stays tied to adapter output instead of a hand-maintained schema description, which is the right fit for BP-03/BP-04 freeze discipline (`test/perf-datasets/README.md:64-80`).
- The focused Bun suite covers the two approval-critical behaviors for this lane: deterministic regeneration and parser-backed manifest validation (`test/perf-datasets/scale-datasets.test.ts:24-82`).

## drift

- No blocking drift found in the allowed review surface.
- Informational only: the mixed scenario currently covers Codex plus Claude Code, not Cursor shared-DB data. The packet only required Codex-heavy, Claude Code-heavy, and one mixed rich-adapter scenario if it fit cleanly, so this does not block approval (`docs/execution/tasks/W3-SCALE-01-deterministic-scale-datasets.md:62-65`).

## unowned spread

- None attributable to `W3-SCALE-01`.
- The wider repo has concurrent changes in other lanes, but this packet’s reviewed diff stayed inside `scripts/perf-datasets/**`, `test/perf-datasets/**`, and packet-local audit/control-plane artifacts.

## progress

- Packet state remains `review_ready` at review time (`.execution/packets/W3-SCALE-01.md:3-13`).
- This review closes the approval gate from the reviewer side and updates the blueprint scoreboard accordingly.

## Codex decisions needed

- None.
- Move `W3-SCALE-01` to `approved`.
