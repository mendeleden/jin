# Review — `W3-V2-01`

- reviewer: `codex`
- session: `codex-REVIEWER-v2-final-steps`
- date: `2026-04-07`

## verdict

`needs_codex` — `W3-V2-01` cannot move to `approved` yet because the final-steps
audit still treats `W3-RUNTIME-01` as pending even though runtime is already
approved and committed in `45529f8`.

## scope of review

- Re-read the required execution docs, live control plane, packet state, worker
  heartbeat, prior runtime recheck, packet-owned audit/task docs, and the
  named supporting CLI/source files.
- Verified the current commit with `git rev-parse --short HEAD`.
- Ran `bun src/index.ts help team` and `bun src/index.ts team schema` to verify
  the live operator/developer CLI split and schema path.

## blocking findings

1. The packet's primary deliverable still hardcodes the pre-recheck runtime
   gate, so the checklist no longer matches the approved packet state and
   cannot serve as the release-prep sequence that `W3-V2-01` is supposed to
   deliver. `docs/execution/audits/2026-04-07-W3-V2-01-final-steps.md:8-31`
   says `W3-RUNTIME-01` is still `review_ready` and requires re-review,
   approval, packet updates, and commit before E2E or preview. The same stale
   assumption repeats in the readiness split and completion report at
   `docs/execution/audits/2026-04-07-W3-V2-01-final-steps.md:89-103` and
   `docs/execution/audits/2026-04-07-W3-V2-01-final-steps.md:151-162`. That
   conflicts with the live control plane and repo state:
   `.execution/program.md:5,14-15`,
   `.execution/packets/W3-RUNTIME-01.md:5,11-13,50-53`,
   `.execution/packets/W3-V2-01.md:12,25-27`,
   `.execution/reviews/2026-04-07-W3-RUNTIME-01-codex-recheck.md:7-10,24-40`,
   and `git rev-parse --short HEAD` returning `45529f8`. Because the audit is
   the packet-owned checklist that Codex would use to move toward E2E/preview,
   this stale first gate blocks `W3-V2-01` approval.

## BP Acceptance Matrix verification

- Runtime/store cutover state is explicit and current: failed. The audit makes
  runtime state explicit, but it does so with the obsolete pre-approval
  `review_ready` state instead of the actual approved-and-committed state in
  `.execution/program.md:5,14-15`,
  `.execution/packets/W3-RUNTIME-01.md:5,11-13,50-53`, and
  `.execution/reviews/2026-04-07-W3-RUNTIME-01-codex-recheck.md:7-10,24-40`.
- Binary rebuild/install prerequisite: verified. The audit keeps rebuild,
  install, smoke, and service-entrypoint verification distinct at
  `docs/execution/audits/2026-04-07-W3-V2-01-final-steps.md:33-46`, which
  matches `package.json:8-15`, `src/commands/start.ts:24-57`, and
  `src/commands/service.ts:16-46,383-445`.
- Local Docker/Postgres E2E prerequisite and BP-09 boundary: verified. The
  audit keeps operator bootstrap under `jin team schema` / `jin team bridge`
  and developer onboarding under `jin connect --team=<code>` at
  `docs/execution/audits/2026-04-07-W3-V2-01-final-steps.md:48-69`; that
  matches `src/commands/schema.ts:113-135,230-237`,
  `src/commands/connect.ts:73-107`, `src/index.ts:282-297,727-733`, and the
  live output from `bun src/index.ts help team` plus `bun src/index.ts team schema`.
- Local binary smoke remains distinct from the generic integration harness and
  the persona E2E gate: verified. The audit separates installed-binary smoke
  from Docker/Postgres persona work at
  `docs/execution/audits/2026-04-07-W3-V2-01-final-steps.md:33-69`; that
  matches `test/docker-compose.integration.yml:1-28`,
  `test/integration.test.ts:1-5,530-620`, and `src/commands/status.ts:22-87`.
- Preview caveats are explicit and do not overclaim full v2 completion:
  verified. The audit keeps the `LegacyStore` TUI defer, broader
  `src/store.ts` spread, repo-wide typecheck debt, and the generic scope of
  `test/integration.test.ts` explicit at
  `docs/execution/audits/2026-04-07-W3-V2-01-final-steps.md:71-103`, which is
  consistent with the approved runtime recheck at
  `.execution/reviews/2026-04-07-W3-RUNTIME-01-codex-recheck.md:76-77,104-107`.

## aligned

- The operator/developer split is preserved in both docs and live CLI output:
  `jin team` is still the operator surface, and developers still join with
  `jin connect --team=<code>`.
- The rebuild/install guidance is operationally consistent with the current
  service path: `jin start --service` delegates to `jin service install`, and
  `service.ts` resolves the running executable before falling back to common
  install paths.
- The audit correctly treats `bun run test:integration` as generic sink
  plumbing, not as a substitute for the persona-based `W3-E2E-01` flow.

## drift

- `docs/execution/tasks/W3-V2-01-final-steps-before-e2e.md:79-84` still says
  the first blocker remains `W3-RUNTIME-01` approval/commit.
- `.execution/agents/codex-WORKER-v2-final-steps.md:30-34` still says
  `W3-RUNTIME-01` is `review_ready`.
- `.execution/blueprints.md` had stale BP-05 wording implying the runtime
  packet could merely "move to approved"; I updated the reviewer-owned
  scoreboard to reflect that `W3-RUNTIME-01` is already approved and committed
  in `45529f8` and that the remaining issue is the docs-only `W3-V2-01`
  checklist drift.

## unowned spread

- `src/tui/app.tsx` retaining `LegacyStore` and the broader non-packet
  `src/store.ts` compatibility spread remain prior BP-05 audit debt outside
  this review-only lane.
- I did not reopen product-code changes or unrelated worktree changes outside
  the allowed review files.

## progress

- `git rev-parse --short HEAD` returned `45529f8`.
- `bun src/index.ts help team` confirmed the operator-only `jin team` surface
  and developer onboarding via `jin connect --team=<code>`.
- `bun src/index.ts team schema` confirmed the schema escape hatch still lives
  under `jin team schema`.

## Codex decisions needed

- Do not move `W3-V2-01` to `approved` yet.
- Refresh the docs-only packet outputs so the final-steps checklist starts from
  `W3-RUNTIME-01` already being approved and committed in `45529f8` instead of
  instructing a now-stale re-review/commit loop. At minimum this affects
  `docs/execution/audits/2026-04-07-W3-V2-01-final-steps.md`; the stale task
  execution note and worker heartbeat should be cleaned up at the same time.
- After that refresh, a narrow review recheck should be sufficient. No new
  product-code investigation is needed from this lane.
