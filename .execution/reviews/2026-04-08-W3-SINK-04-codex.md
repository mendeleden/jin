# W3-SINK-04 Codex Review (2026-04-08)

## verdict

- `approved` not recommended yet.
- One blocking finding remains: the packet still lacks the clean-start local + remote row-count proof required for approval.

## scope of review

- Session: `codex-REVIEWER-postgres-push-regression`
- Reviewed control-plane and packet state:
  - `.execution/program.md`
  - `.execution/packets/W3-SINK-04.md`
  - `.execution/agents/codex-WORKER-postgres-push-regression.md`
- Reviewed packet-owned implementation and evidence:
  - `src/sinks/postgres.ts`
  - `test/postgres-reference-sink.test.ts`
  - `docs/execution/audits/2026-04-08-W3-SINK-04-postgres-push-regression-and-release-sink-validation.md`
- Reviewed in-scope blueprints:
  - `docs/blueprint/BP-02-data-flow.md`
  - `docs/blueprint/BP-05-store-and-migration.md`
  - `docs/blueprint/BP-06-sink-contract.md`
  - `docs/blueprint/BP-10-performance-validation.md`
- Validation command executed:
  - `bun test test/postgres-reference-sink.test.ts` (pass: `6/6`)

## blocking findings

- Missing approval evidence:
  - The packet requires a clean-start artifact that records local store counts, local Postgres row counts, Railway row counts, and `_jin_push_state` outcome (`docs/execution/tasks/W3-SINK-04-postgres-push-regression-and-release-sink-validation.md:73-87`, `97-113`, `137-142`).
  - The current audit records the exact rerun commands and the sandbox connectivity failures, but no post-fix local/remote row counts or representative successful `_jin_push_state` rows (`docs/execution/audits/2026-04-08-W3-SINK-04-postgres-push-regression-and-release-sink-validation.md:54-133`).
  - This keeps the packet incomplete for approval even though the code fix and focused regression look correct.

## BP Acceptance Matrix verification

- Postgres sink still obeys BP-06 ownership (`implemented`):
  - The fix stays inside sink transport execution and does not change pipeline eligibility, payload shape, or retry semantics (`src/sinks/postgres.ts:147-166`, `335-452`).
- Bun SQL transport regression is covered (`implemented`):
  - New regression test proves root-client `unsafe(...)` can throw while schema checks and DML still succeed through `sql.begin(...)` (`test/postgres-reference-sink.test.ts:142-194`).
- Clean-start local + remote row delivery proof (`not yet satisfied`):
  - Audit includes exact rerun commands, but no successful local or Railway row counts are recorded yet (`docs/execution/audits/2026-04-08-W3-SINK-04-postgres-push-regression-and-release-sink-validation.md:54-133`).
- `_jin_push_state` records success/failure correctly after the fix (`not yet revalidated end-to-end`):
  - The packet requests representative `_jin_push_state` evidence from the clean-start run, and that artifact is still missing (`docs/execution/tasks/W3-SINK-04-postgres-push-regression-and-release-sink-validation.md:102-104`, `137-142`).

## V1 comparison

- parity preserved at the sink contract surface.
- The fix only changes how `postgres://` transport queries execute; it does not change sink interface semantics or pipeline ownership.

## aligned

- RCA is concrete and matches the reported runtime error.
- Diff is narrow and stays inside packet-owned files.
- Focused regression coverage passes and directly exercises the failure class.

## drift

- Release-facing sink proof is still pending.
- This is an acceptance gap, not a new code-level drift finding in the reviewed fix.

## progress

- The code path behind `Only use sql.begin, sql.reserved or max: 1` is fixed and regression-tested.
- `W3-SINK-04` should move to `needs_codex` until unrestricted validation captures the required row-count and `_jin_push_state` evidence.

## Codex decisions needed

- Rerun the disposable clean-start validation on an unrestricted host.
- Capture:
  - local SQLite counts
  - local Postgres row counts
  - Railway row counts
  - representative successful `_jin_push_state` rows
- Re-review for approval once that artifact exists.
