# W3-TEAM-01 Codex Review

## verdict

- `needs_codex`
- One blocking finding remains: after a successful `jin team schema apply`, the live CLI still tells the operator to run `jin sink add postgres ...` (`src/commands/schema.ts:148-159`). That reintroduces low-level sink wiring into the operator bootstrap path that BP-09 narrows to `jin team bridge` plus `jin team schema ...` (`docs/blueprint/BP-09-cli-split.md:127-137`, `docs/blueprint/BP-09-cli-split.md:245-266`, `docs/blueprint/BP-09-cli-split.md:318-337`).

## scope of review

- Read in order:
  - `docs/execution/00-global-rules.md`
  - `docs/execution/01-dispatch-protocol.md`
  - `docs/execution/05-live-control-plane.md`
  - `docs/execution/tasks/W3-TEAM-01-team-bootstrap-and-schema-escape-hatch.md`
- Read live control plane:
  - `.execution/program.md`
  - `.execution/blueprints.md`
  - `.execution/packets/W3-TEAM-01.md`
  - `.execution/agents/claude-WORKER-team-bootstrap.md`
  - `.execution/packets/W3-PRODUCT-01.md`
  - `docs/blueprint/BP-09-cli-split.md`
- Read packet-owned BP/code/tests:
  - `docs/blueprint/BP-Product-Strategy.md`
  - `docs/blueprint/BP-01-module-map.md`
  - `docs/blueprint/BP-05-store-and-migration.md`
  - `docs/blueprint/BP-06-sink-contract.md`
  - `docs/blueprint/BP-08-routing-and-config.md`
  - `src/index.ts`
  - `src/commands/team-config.ts`
  - `src/commands/connect.ts`
  - `src/commands/init.ts`
  - `src/commands/schema.ts`
  - `src/sinks/postgres.ts`
  - `test/team-bootstrap.test.ts`
  - `test/init.test.ts`
  - `test/connect.test.ts`
  - `test/config-mutation-control.test.ts`
- Ran allowed verification commands:
  - `bun src/index.ts --help`
  - `bun src/index.ts help team`
  - `bun src/index.ts team schema`
  - `bun test test/team-bootstrap.test.ts`
  - `bun test test/init.test.ts test/connect.test.ts test/config-mutation-control.test.ts`

## blocking findings

1. `src/commands/schema.ts:148-159` still routes the operator from `jin team schema apply` into `jin sink add postgres --connection-string=...` after a successful apply. BP-09's initial operator surface is `jin team bridge` plus `jin team schema ...`, and its onboarding flow keeps operators inside `jin team` while developers use `jin connect --team=<code>` (`docs/blueprint/BP-09-cli-split.md:127-137`, `docs/blueprint/BP-09-cli-split.md:245-266`, `docs/blueprint/BP-09-cli-split.md:318-337`, `docs/blueprint/BP-09-cli-split.md:451-464`). BP-Product likewise separates operator commands from generic integration wiring (`docs/blueprint/BP-Product-Strategy.md:283-300`, `docs/blueprint/BP-Product-Strategy.md:304-325`). Main help and dispatch are corrected, but this success path is still live operator guidance, so the packet does not yet satisfy the review goal that operator bootstrap be limited to `jin team bridge` and `jin team schema ...`. The focused tests do not cover this branch; `test/team-bootstrap.test.ts:159-224` only covers help, dry-run, and DDL constants.

No other blockers found in the reviewed surface.

## BP Acceptance Matrix verification

- Team remains a distinct product plane in the local CLI surface, not a renamed Postgres sink flow -> implemented. Evidence: `src/index.ts:281-365`, `src/index.ts:678-757`, `src/commands/team-config.ts:23-43`, `src/commands/team-config.ts:69-88`, `test/team-bootstrap.test.ts:96-136`, `test/team-bootstrap.test.ts:252-267`, plus live help from `bun src/index.ts --help` and `bun src/index.ts help team`.
- Developer onboarding stays at top-level `jin connect --team=<code>` rather than moving under `jin team` -> implemented. Evidence: `src/index.ts:116-141`, `src/index.ts:335-350`, `src/index.ts:435-458`, `src/commands/connect.ts:73-84`, `src/commands/connect.ts:483-486`, `test/connect.test.ts:145-169`, `test/team-bootstrap.test.ts:141-154`.
- Any remote Postgres bootstrap path is explicit operator/admin surface under `jin team schema ...`, not the default onboarding story -> blocking drift. Evidence for the corrected surface: `src/index.ts:281-307`, `src/index.ts:678-737`, `src/commands/schema.ts:107-145`, `test/team-bootstrap.test.ts:159-245`, `bun src/index.ts team schema`. Blocking counter-evidence: `src/commands/schema.ts:148-159` still exits through `jin sink add postgres ...`.
- Existing workspace onboarding (`team-config`, `connect --team`, `init --team`) does not regress -> implemented. Evidence: `src/index.ts:201-222`, `src/index.ts:754-770`, `src/commands/connect.ts:73-84`, `src/commands/init.ts:24-50`, `src/commands/init.ts:161-178`, `test/init.test.ts:89-152`, `test/connect.test.ts:145-169`, `test/config-mutation-control.test.ts:255-315`.
- Generic sink wiring stays separate from Team/product framing -> blocking drift for the same success-path leak above. Evidence for separation elsewhere: `src/index.ts:128-141`, `src/index.ts:255-280`, `src/index.ts:321-350`, `src/commands/team-config.ts:78-81`, `test/config-mutation-control.test.ts:255-315`. Blocking counter-evidence: `src/commands/schema.ts:158-159`.
- `jin team init` / `jin team status` remain deferred unless workspace identity is real and non-heuristic -> implemented. Evidence: `src/index.ts:292-297`, `test/team-bootstrap.test.ts:270-275`, live `bun src/index.ts help team`. `jin team connect` and top-level `jin schema` are also absent from the reviewed help/dispatch surface (`src/index.ts:321-350`, `src/index.ts:678-757`).

## V1 comparison

- Parity kept: developer onboarding is still `jin connect --team=<code>`, backed by the existing connect handler that decodes the bridge code and routes repos by remote (`src/index.ts:116-141`, `src/index.ts:435-458`, `src/commands/connect.ts:73-84`, `src/commands/connect.ts:559-600`, `test/connect.test.ts:145-169`).
- Parity kept: compatibility onboarding helpers remain. Top-level `jin init --team` still configures the workspace sink and nudges into repo routing, and top-level `jin team-config` survives as a deprecated alias (`src/index.ts:201-222`, `src/index.ts:754-770`, `src/commands/init.ts:24-50`, `src/commands/init.ts:164-178`, `test/init.test.ts:89-152`).
- Intentional BP-backed change: explicit operator/admin surfaces are now `jin team bridge` and `jin team schema apply|check|version`; top-level `jin schema` and `jin team connect` are not part of the live help/dispatch surface (`src/index.ts:281-307`, `src/index.ts:321-350`, `src/index.ts:678-757`, `test/team-bootstrap.test.ts:96-170`, `test/team-bootstrap.test.ts:261-275`, plus the three live help commands above).
- Not fully accurate yet: the handoff claim that operator bootstrap is limited to `jin team` is overstated until `schemaApplyCommand()` stops pointing operators to `jin sink add postgres` after apply (`src/commands/schema.ts:148-159`).

## aligned

- `bun src/index.ts --help` shows `connect --team=<code>` in the developer `Connect` section, keeps `team <subcommand>` in a separate `Workspace (operator)` section, and does not expose a top-level `schema` command.
- `bun src/index.ts help team` shows only `bridge`, `schema apply|check|version`, and deferred `init` / `status`. It does not expose `team connect`.
- `bun src/index.ts team schema` exposes only `apply|check|version`.
- Focused tests passed:
  - `bun test test/team-bootstrap.test.ts` -> 14 pass, 0 fail
  - `bun test test/init.test.ts test/connect.test.ts test/config-mutation-control.test.ts` -> 42 pass, 0 fail

## drift

- Blocking drift: `src/commands/schema.ts:148-159` still points operators to `jin sink add postgres ...` after `jin team schema apply`.
- Non-blocking naming drift: the implementation keeps `src/commands/team-config.ts` and `src/commands/schema.ts` instead of the BP-09 module-map sketch (`docs/blueprint/BP-09-cli-split.md:341-362`). Packet ownership explicitly allowed the existing files and optional new entrypoints, so this is not an approval blocker by itself.

## unowned spread

- None detected. Current packet worktree changes are confined to owned files: `src/index.ts`, `src/commands/team-config.ts`, `src/commands/schema.ts`, and `test/team-bootstrap.test.ts`.

## progress

- Review completed.
- Main BP-09 correction is real: `jin team connect` and top-level `jin schema` are gone from the live surface, and `jin team init` / `jin team status` are explicitly deferred.
- `W3-TEAM-01` should not move to `approved` yet because the operator success path in `schemaApplyCommand()` still leaks back into generic sink wiring.

## Codex decisions needed

- Preferred: patch `src/commands/schema.ts:158-159` so the post-apply guidance stays inside the Team/operator surface, for example by pointing to `jin team bridge --type=postgres ...` (or `jin team help`) instead of `jin sink add postgres ...`, then rerun the allowed help commands and focused tests.
- Alternative: if Codex intentionally wants `jin team schema apply` to hand operators back to generic sink wiring, record that as an explicit BP-09/BP-Product defer or blueprint amendment before approval. Without that explicit decision, this packet should remain out of `approved`.
