# Packet State

- packet: `W3-UI-01`
- title: `Remove TUI and SPA Dashboard Surface`
- status: `blocked`
- assigned agent: `unassigned`
- branch: `feat/rewrite-ontology`
- worktree/container: `canonical repo workspace` / `local`
- depends on: `W3-RUNTIME-01`
- unblocks: `simpler binary`, `cleaner CLI surface`, `future desktop/query redesign without legacy UI baggage`
- last transition: `2026-04-08`
- next Codex action: `leave blocked unless we need the narrower UI-only slice again; active execution moved to W3-CLEANUP-01`
- latest review: `none`

## Notes

- user direction is to remove the current TUI, SPA dashboard, and bindings for it
- this packet should not redesign the future desktop app or daemon query mechanism
- broader removal work now lives in `W3-CLEANUP-01`, which absorbed the UI lane
  because the requested cleanup also includes onboarding/store compatibility
  bridges outside this packet's original owned files
- keep future-friendly route/control library code only if it is not part of the
  current SPA/dashboard surface
