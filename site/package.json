{
  "name": "jin-site",
  "private": true,
  "type": "module",
  "scripts": {
    "copy:install": "mkdir -p public && cp ../install.sh ../install.ps1 public/",
    "dev": "bun run copy:install && astro dev",
    "build": "bun run copy:install && astro build",
    "preview": "astro preview"
  },
  "devDependencies": {
    "@astrojs/starlight": "^0.39.2",
    "astro": "^6.4.2"
  }
}
