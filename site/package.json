{
  "private": true,
  "type": "module",
  "scripts": {
    "copy:install": "mkdir -p docs/public && cp ../install.sh ../install.ps1 docs/public/",
    "docs:dev": "bun run copy:install && vitepress dev docs",
    "docs:build": "bun run copy:install && vitepress build docs",
    "docs:preview": "vitepress preview docs"
  },
  "devDependencies": {
    "vitepress": "^1.6.4"
  }
}
